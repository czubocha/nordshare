version: 0.2
phases:
  install:
    runtime-versions:
      golang: 1.14
  build:
    commands:
      - sam validate -t deployments/backend.yml
      - sam validate -t deployments/cloudfront.yml
      - go vet ./...
      - go test ./...
